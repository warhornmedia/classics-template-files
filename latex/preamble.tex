% DEFINE PHYSICAL DOCUMENT SETTINGS
% media size
\usepackage[paperwidth=5in, paperheight=8in]{geometry}
% margins
\geometry{left=.75in}
\geometry{right=.75in}
\geometry{bottom=.75in}
\geometry{top=.75in}

% define link colors
\usepackage{hyperref}
\definecolor{WarhornTeal}{RGB}{26, 169, 171}
\hypersetup{
    colorlinks=true,
    linkcolor=WarhornTeal,
    filecolor=WarhornTeal,
    urlcolor=WarhornTeal,
}

% Start of default stuff from bookdown
% format tables for a book
\usepackage{booktabs}

% typeset theorems
\usepackage{amsthm}

% fixes spacing or something with theorems, I think
\makeatletter
\def\thm@space@setup{%
  \thm@preskip=8pt plus 2pt minus 4pt
  \thm@postskip=\thm@preskip
}
\makeatother
% End of default stuff from bookdown

% Insert book cover first and format title page after that
\usepackage{titling}
\usepackage{pdfpages}
\IfFileExists{./cover.pdf}{
  \newcommand{\myCover}{./cover.pdf}}
  {\IfFileExists{./cover.jpg}{
    \newcommand{\myCover}{./cover.jpg}}
    {\IfFileExists{./cover.png}{
      \newcommand{\myCover}{./cover.png}}{}
    }
  }
\makeatletter
\@ifundefined{myCover}
{}
{
\pretitle{\begin{center}\includepdf{\myCover}}
\posttitle{\end{center}\setcounter{page}{0}}
\usepackage{atbegshi}% http://ctan.org/pkg/atbegshi
\AtBeginDocument{\AtBeginShipoutNext{\AtBeginShipoutDiscard}}
}
\makeatother

% poetry styling
\newenvironment{poetry}[0]{\par\leftskip=2em\rightskip=2em}{\par\medskip}

% specify fonts
%\babelfont[*Latin]{rm}{Calluna}
%\babelprovide[import]{greek}
%\babelfont[*greek]{rm}{Baskerville}

\usepackage{fontspec}
\setmainfont{Baskerville}

%specify that what comes next is frontmatter, thus page nums are roman numerals
\frontmatter
